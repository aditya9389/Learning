const superHeroes: String[] = ["Thor", "Spiderman", "Ironman"];
const superPower: Array<String> = ["Flight", "Strength", "Speed"];  //same thing
const temp: number[]=[];
temp.push(23);

type User={
    name: string,
    isActive: boolean
}
const allUsers: User[]=[];
allUsers.push({name: "Adii", isActive: false});
allUsers.push({name: "John", isActive: true});

const MLModels: number[][]=[[255,255,236]];
MLModels.push([255,255,255]);
MLModels.push([123,456,789]);  

console.log(MLModels);
export {};